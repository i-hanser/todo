TODO List 项目说明文档

1. 技术选型

编程语言
TypeScript：选择 TypeScript 而非纯 JavaScript，理由：
- 提供类型安全，减少运行时错误
- 更好的 IDE 支持和代码提示
- 便于维护和扩展

框架/库
React 18：选择 React 作为 UI 框架，理由：
- 组件化开发，代码结构清晰
- 丰富的生态系统和社区支持
- Hooks API 简洁，状态管理方便
- 虚拟 DOM 提升性能

Vite：选择 Vite 作为构建工具，理由：
- 开发服务器启动速度快（基于 ES modules）
- 热更新（HMR）响应迅速
- 配置简单，开箱即用
- 生产构建使用 Rollup，性能优秀

数据库/存储
localStorage：选择浏览器 localStorage 作为数据存储，理由：
- 无需后端服务器，部署简单
- 数据持久化，刷新页面不丢失
- 零配置，浏览器原生支持
- 适合单用户、单设备场景

替代方案对比：
- IndexedDB：功能更强大但复杂度高，本项目数据量小，localStorage 足够
- 后端数据库：需要服务器和 API，增加部署成本，不符合题目要求的快速实现
- 文件存储：Web 应用无法直接访问文件系统

2. 项目结构设计

整体架构
本项目采用前端单页应用（SPA）架构：
- 纯前端应用：所有逻辑在浏览器端执行
- 组件化设计：主组件 App.tsx 包含所有功能逻辑
- 状态管理：使用 React Hooks（useState, useEffect）管理状态
- 数据持久化：通过 localStorage API 实现

目录结构
todo/
├── src/
│   ├── App.tsx          # 主应用组件（包含所有业务逻辑）
│   ├── App.css          # 组件样式文件
│   ├── main.tsx         # 应用入口文件
│   └── index.css        # 全局样式文件
├── index.html           # HTML 模板
├── package.json         # 项目依赖和脚本配置
├── tsconfig.json        # TypeScript 编译配置
├── tsconfig.node.json   # Node.js 环境 TypeScript 配置
├── vite.config.ts       # Vite 构建工具配置
├── .gitignore          # Git 忽略文件配置
├── README.md           # 项目快速开始指南
└── DOC.md              # 项目详细说明文档（本文件）

模块职责说明
- App.tsx：核心组件，包含：
  - 待办事项的增删改查逻辑
  - 状态管理（todos、编辑状态、搜索等）
  - 数据持久化（localStorage 读写）
  - UI 渲染和事件处理
- App.css：组件样式，包含 Morandi 色系设计
- main.tsx：应用入口，渲染根组件
- index.css：全局样式重置和基础样式

3. 需求细节与决策

描述字段处理
- 是否必填：描述字段为可选，允许为空
- 空输入处理：空字符串转换为 undefined，不存储空值
- 显示逻辑：无描述时显示占位文本"点击添加描述..."

已完成任务显示

在 UI 中，已完成任务的显示方式经过精心设计，确保用户能够清晰地区分已完成和待完成的任务：

视觉样式设计：
1. 标题样式变化：
   - 标题文字添加删除线（text-decoration: line-through），直观表示任务已完成
   - 文字颜色从 #6B6B6B 变为 #B8B8B8，降低视觉权重
   - 鼠标悬停时不再显示背景色变化，表示已完成任务不可编辑

2. 复选框状态：
   - 复选框显示为已勾选状态（checked）
   - 点击复选框可以取消完成状态，将任务重新标记为待完成

3. 其他字段显示：
   - 描述、分类、截止日期等字段保持显示，但颜色变淡
   - 截止日期如果已过期，不再显示红色警告样式（因为已完成）
   - 所有字段在已完成状态下不可编辑，点击无反应

分组显示机制：
- 已完成任务与待完成任务完全分离，显示在独立的"已完成"分组中
- "已完成"分组默认折叠（collapsed），不占用过多屏幕空间
- 用户可以点击分组标题展开/折叠，查看历史完成记录
- 分组标题显示已完成任务数量，方便快速了解完成情况

统计信息展示：
- 在待办列表顶部显示统计信息："共 X 项，已完成 Y 项"
- 当有搜索条件时，统计信息会同时显示搜索结果数量和总数
- 统计信息帮助用户快速了解整体进度

交互行为：
- 已完成任务不支持拖拽排序（拖拽手柄不显示）
- 已完成任务不支持编辑操作（标题、描述、日期、分类均不可编辑）
- 已完成任务可以删除，删除操作与其他任务相同
- 已完成任务可以通过取消勾选复选框重新激活为待完成状态

设计理由：
这种设计方式符合用户心理模型：已完成的任务应该"淡出视野"但保留历史记录，待完成的任务应该突出显示并支持各种操作。分组显示避免了列表过长的问题，折叠机制让用户专注于当前需要处理的任务。

任务排序逻辑

本项目没有采用传统的"按创建时间排序"方式，而是采用了更符合实际使用场景的多维度排序策略：

默认排序规则（三级排序机制）：

第一优先级：按分类排序
- 排序顺序：工作 > 学习 > 生活 > 无分类
- 设计理由：用户通常按项目或领域组织任务，同一分类的任务放在一起更便于管理
- 实现方式：为每个分类分配数字权重（工作=1，学习=2，生活=3，无分类=4），数字越小优先级越高

第二优先级：自定义顺序（order 字段）
- 当用户通过拖拽调整了任务顺序后，系统会为任务分配 order 值
- order 值越小，任务越靠前
- 如果任务有自定义顺序，优先使用自定义顺序，忽略后续的日期排序
- 实现方式：拖拽时计算插入位置的 order 值，使用浮点数确保插入位置精确

第三优先级：按截止日期排序
- 仅在任务没有自定义顺序时生效
- 有截止日期的任务按日期升序排列（越早越靠前）
- 没有截止日期的任务排在最后
- 实现方式：比较 Date 对象的 getTime() 值

为什么不按创建时间排序：
1. 实际使用中，用户更关心任务的紧急程度（截止日期）而非创建时间
2. 按分类组织更符合项目管理习惯
3. 创建时间信息在本项目中未存储，需要额外实现
4. 截止日期是更直观的优先级指标

自定义排序功能：
- 拖拽排序：用户可以通过拖拽任务左侧的拖拽图标调整顺序
- 排序范围限制：只能在相同分类和相同完成状态的任务之间拖拽
  - 理由：保持分类分组的一致性，避免混乱
  - 实现：拖拽时检查 draggedTodo 和 targetTodo 的 category 和 completed 字段
- 视觉反馈：拖拽时显示半透明效果，悬停位置高亮显示
- 持久化：自定义顺序通过 order 字段保存到 localStorage

重置排序功能：
- 位置：显示在"待完成"分组标题旁边
- 功能：清除所有任务的自定义顺序（order 字段）
- 触发条件：只有当存在自定义排序时才显示按钮
- 效果：重置后，任务恢复为按分类和截止日期的自动排序

排序逻辑的代码实现：
排序函数 sortTodosByCategoryAndDeadline 采用多条件比较：
1. 首先比较分类顺序（categoryOrder）
2. 分类相同时，比较自定义顺序（order 字段）
3. 都没有自定义顺序时，比较截止日期（deadline）
4. 都没有截止日期时，保持原顺序

这种排序方式既保证了自动排序的合理性，又给用户提供了灵活的自定义空间。

扩展功能设计思路

搜索功能
实现方式：实时搜索，使用 includes() 方法进行模糊匹配
- 搜索范围：标题、描述、分类三个字段
- 分词搜索：支持多关键词搜索（空格分隔），使用 AND 逻辑（必须同时包含所有关键词）
- 高亮显示：搜索结果中关键词高亮显示

设计细节：
- 实时搜索：用户输入时立即过滤结果，无需点击搜索按钮
- 大小写不敏感：搜索时将所有文本转换为小写进行比较
- 多字段合并：将标题、描述、分类合并为一个字符串进行搜索，提高搜索覆盖率
- 关键词高亮：使用正则表达式匹配关键词，用 <mark> 标签包裹高亮显示
- 空结果提示：当搜索无结果时，显示友好的提示信息

内联编辑
交互方式：点击字段即可编辑，无需额外按钮
- 保存方式：失焦（blur）或按 Enter 键保存
- 取消编辑：按 Escape 键取消
- 防冲突：同时只能编辑一个字段，切换编辑时自动保存上一个

设计细节：
- 无缝切换：点击任意字段即可进入编辑模式，用户体验流畅
- 自动保存：切换编辑字段时，自动保存上一个字段的修改
- 取消机制：按 Escape 键可以取消当前编辑，恢复原值
- 事件处理：使用 stopPropagation() 防止事件冒泡，确保点击编辑区域不会触发其他操作
- 焦点管理：编辑时自动聚焦到输入框，提升操作效率

拖拽排序
实现方式：使用 HTML5 Drag and Drop API
- 拖拽区域：左侧拖拽图标，避免误触
- 视觉反馈：拖拽时显示半透明效果，悬停位置高亮
- 排序范围：仅在相同完成状态和分类内拖拽

设计细节：
- 拖拽手柄：使用 SVG 图标作为拖拽手柄，视觉清晰，避免误操作
- 范围限制：只能在同分类、同完成状态的任务间拖拽，保持列表结构清晰
- 顺序计算：使用浮点数 order 值，支持在任意位置插入，无需重新计算所有任务的顺序
- 视觉反馈：拖拽时添加 dragging 和 drag-over 类，提供清晰的视觉反馈

提醒/通知功能设计思路（已实现但后来移除）

虽然提醒功能最终被移除，但在设计阶段有完整的实现方案：

设计目标：
- 在任务截止日期临近或到期时提醒用户
- 使用浏览器原生通知 API，无需后端支持
- 支持自定义提前提醒时间

实现方案：
1. 权限管理：
   - 使用 Notification API 请求浏览器通知权限
   - 处理三种权限状态：granted（已授权）、denied（已拒绝）、default（未询问）
   - 权限被拒绝时提示用户在浏览器设置中开启

2. 定时检查机制：
   - 使用 setInterval 每分钟检查一次待办事项
   - 检查未完成且有截止日期的任务
   - 计算距离截止日期的天数

3. 提醒时机：
   - 提前提醒：在截止日期前 N 天提醒（N 可配置，默认 1 天）
   - 当天提醒：截止日期当天提醒
   - 过期提醒：截止日期已过但任务未完成时提醒

4. 防重复通知：
   - 使用 localStorage 记录已通知的任务 ID
   - 每个任务在每个提醒时机只通知一次
   - 任务完成或删除后清除通知记录

5. 通知内容：
   - 标题：固定为"待办提醒"
   - 内容：任务标题 + 提醒信息（如"还有 1 天到期"）
   - 点击通知：聚焦到浏览器窗口

移除原因：
- 用户需求变更，不需要通知功能
- 简化应用功能，专注于核心的待办管理

多设备同步功能设计思路（未实现）

如果需要实现多设备同步，可以考虑以下方案：

方案一：后端 API + 数据库
- 架构：前端 + RESTful API + 数据库（如 PostgreSQL）
- 实现：
  - 用户注册/登录系统
  - 待办数据存储在服务器数据库
  - 前端通过 API 同步数据
  - 使用 WebSocket 实现实时同步
- 优点：数据安全，支持多用户，功能完整
- 缺点：需要服务器，开发成本高

方案二：Firebase / Supabase
- 架构：前端 + Firebase/Supabase 后端服务
- 实现：
  - 使用 Firebase Authentication 处理用户认证
  - 使用 Firestore 存储待办数据
  - 使用实时监听实现数据同步
- 优点：无需自建服务器，开发快速
- 缺点：依赖第三方服务，可能有成本

方案三：PWA + IndexedDB + Service Worker
- 架构：渐进式 Web 应用，离线优先
- 实现：
  - 使用 IndexedDB 存储数据
  - Service Worker 实现离线缓存
  - 通过 Web Share API 或二维码分享数据
- 优点：无需后端，支持离线使用
- 缺点：同步需要手动操作，体验不够流畅

冲突处理策略：
- 最后写入获胜（Last Write Wins）：简单但可能丢失数据
- 操作日志（Operational Log）：记录所有操作，按时间戳合并
- 版本控制：使用版本号检测冲突，需要用户手动解决

考虑到题目要求快速实现和单用户场景，本项目选择了最简单的 localStorage 方案，避免了同步的复杂性。

4. AI 使用说明

使用的 AI 工具Cursor

使用 AI 的环节

Bug 定位与修复
- localStorage 数据丢失问题：AI 帮助定位到初始化时 useEffect 执行顺序问题，引入 isInitialized ref 解决
- 描述字段无法编辑问题：AI 协助分析事件冒泡和焦点问题，通过 stopPropagation() 和样式调整解决
- 拖拽功能不生效：AI 帮助检查事件处理和状态更新逻辑

功能实现
- 搜索功能：AI 提供分词搜索的实现思路和代码
- 拖拽排序：AI 协助实现 HTML5 Drag and Drop API 的完整逻辑
- 日期格式化：AI 提供相对时间显示的实现方案

文档编写
- README.md 和 DOC.md 的初稿由 AI 生成，然后根据实际情况调整

AI 输出的修改与优化
- 代码结构：AI 生成的代码有时过于复杂，简化了部分逻辑，保持代码简洁
- 样式设计：AI 提供的样式基础上，调整了颜色搭配和间距，使其更符合 Morandi 色系
- 注释风格：根据要求精简注释，只保留必要说明，模仿初入职场学生的风格
- 功能取舍：AI 建议添加通知功能，但后来根据需求移除了该功能

5. 运行与测试方式

本地运行方式

安装依赖
npm install

启动开发服务器
npm run dev
启动后访问 http://localhost:5173（端口可能因占用而自动调整）

构建生产版本
npm run build
构建产物输出到 dist 目录

预览生产版本
npm run preview

已测试环境
- Node.js: v16.19.1（初始版本，后因 Vite 5 不兼容降级到 Vite 4.5.0）
- 操作系统: macOS 24.6.0
- 浏览器: Chrome（主要测试浏览器）
- 包管理器: npm

已知问题与不足

1. Vite 版本兼容性：
   - 问题：Vite 5.x 需要 Node.js 18+，但环境为 Node.js 16
   - 解决：降级到 Vite 4.5.0
   - 影响：无功能影响，开发体验正常


2. 数据存储限制：
   - localStorage 有 5-10MB 存储限制
   - 仅支持单设备访问
   - 不适合多用户场景

3. 浏览器兼容性：
   - 使用了较新的浏览器 API（如 Drag and Drop）
   - 旧版浏览器可能不支持部分功能

6. 总结与反思

如果有更多时间，会如何改进？

1. 代码结构优化：
   - 将 App.tsx 拆分为多个子组件（TodoItem、TodoForm、SearchBar 等）
   - 提取自定义 Hooks（useTodos、useLocalStorage 等）
   - 使用 Context API 或状态管理库（如 Zustand）管理全局状态

2. 功能增强：
   - 添加任务优先级功能
   - 支持任务标签（Tags）
   - 添加任务归档功能
   - 支持批量操作（批量删除、批量完成）

3. 用户体验优化：
   - 添加动画效果（添加、删除、完成时的过渡动画）
   - 支持键盘快捷键操作
   - 添加撤销/重做功能
   - 优化移动端体验（响应式设计改进）

4. 数据管理：
   - 支持数据导入/导出（JSON 格式）
   - 添加数据备份和恢复功能
   - 支持多设备同步（需要后端支持）

5. 测试与质量：
   - 添加单元测试（使用 Jest + React Testing Library）
   - 添加 E2E 测试（使用 Playwright）
   - 代码质量检查（ESLint、Prettier）

这个实现的最大亮点是什么？

1. 完整的功能实现：
   - 不仅实现了基础功能，还添加了多个扩展功能（搜索、拖拽排序、分类等）
   - 所有功能都经过实际测试，可以正常使用

2. 良好的用户体验：
   - 内联编辑功能，操作流畅自然
   - Morandi 色系设计，视觉舒适
   - 实时搜索和拖拽排序，交互便捷

3. 代码质量：
   - TypeScript 类型安全
   - 代码结构清晰，注释适当
   - 数据持久化实现稳定

4. 快速开发：
   - 使用现代工具链（Vite + React），开发效率高
   - 合理利用 AI 辅助，快速迭代
   - 从零到完整功能实现，开发周期短

5. 实用性强：
   - 功能完整，可直接使用
   - 数据持久化，满足日常使用需求
   - 无需后端，部署简单
